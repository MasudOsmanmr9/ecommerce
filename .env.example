# Basic runtime config
PORT=3000
NODE_ENV=development

# Database (example using DATABASE_URL or separate parts)
DATABASE_URL=postgresql://user:password@localhost:5432/windsurf
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=user
DB_PASSWORD=password
DB_DATABASE=windsurf

# Other
LOG_LEVEL=info
